# paw-example
## This is an example paw package here is what you should refrence too for making packages


# .bark format is going to be disclosed in the file 
> but it allows users to run apps in folders they are in without running py app.py
> it also allows the devs to use the def run(corgi,args): without making a new command
> the users can just type app without typing py app.py
